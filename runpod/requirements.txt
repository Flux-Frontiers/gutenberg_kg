# RunPod GutenbergKG worker — PyPI dependencies
# Non-PyPI packages (gutenberg-kg, kg-rag) are installed
# from local wheels by build_image.sh before docker build runs.

runpod>=1.7.0
doc-kg>=0.12.3
sentence-transformers>=3.0.0
lancedb>=0.29.0
numpy>=1.24.0
httpx>=0.27.0
rich>=14.0.0
click>=8.1.0
pandas>=2.0.0
