# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Sphinx documentation
docs/_build/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
# .python-version

# poetry — include poetry.lock in version control for reproducibility
# poetry.lock

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# mkdocs documentation
/site

# mypy / ty
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff
.ruff_cache/

# PyPI configuration file
.pypirc

# Log files
*.log

# Visual Studio Code
# .vscode/ is tracked to preserve project-specific config (tasks, settings, MCP)

# Local ML model cache (fetched by `gutenkg init`)
.kgcache/

# Corpus snapshots (point-in-time metrics captures)
corpus/.snapshots/

#.mcp.json

# Benchmark working data (LongMemEval corpus + result logs)
benchmarks/data/
benchmarks/results_*.jsonl
benchmarks/results_*.json

# MacOS
.DS_Store
commit.txt

# Large asset files (> 1MB)
assets/*-1024x1024.png
assets/*-xl-*.png
assets/code_kg_arch_*.png

# Generated visualization exports
*_forest_3d.html
*.pyvista.html

entry_*.md
.diary_source.psv
.diary_source_chunks.pkl

# Ignore .claude contents except shared project files: skills and
# settings.json (pre-push ruff hook). Must be dir/* (not dir/) so the
# negations below can re-include — git can't reach into a fully
# ignored directory.
.claude/*
!.claude/skills/
!.claude/settings.json
.venv**
.vscode

# Swift / SwiftPM (app/)
.build/
.swiftpm/
DerivedData/
xcuserdata/

# pdoc output — built in CI (docs.yml), deployed to GitHub Pages
site/

# === KG generated artifacts (normalized across the fleet) ===
# Databases, vector indexes and model caches are transient and
# reproducible. snapshots/ is TRACKED and must never be ignored --
# never use a bare `**/.<ns>/` pattern here, it swallows snapshots too.

**/.pycodekg/lancedb/
**/.pycodekg/lancedb-*
**/.pycodekg/*.sqlite
**/.pycodekg/*.sqlite-shm
**/.pycodekg/*.sqlite-wal
**/.pycodekg/*.db
**/.pycodekg/models

**/.dockg/lancedb/
**/.dockg/lancedb-*
**/.dockg/*.sqlite
**/.dockg/*.sqlite-shm
**/.dockg/*.sqlite-wal
**/.dockg/*.db
**/.dockg/models
**/.dockg/embeddings.json

# AgentKG local store -- nothing under it is ever tracked
**/.agentkg/

**/.diarykg/lancedb/
**/.diarykg/lancedb-*
**/.diarykg/*.sqlite
**/.diarykg/*.sqlite-shm
**/.diarykg/*.sqlite-wal
**/.diarykg/*.db
**/.diarykg/models
**/.diarykg/corpus

# Agent scratch dirs (Claude Code / Codex)
.agents/
.codex/

# MCP server config -- machine-specific, contains absolute paths
.mcp.json
.mcp.json.sav
# === end KG generated artifacts ===
