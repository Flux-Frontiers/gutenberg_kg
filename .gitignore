# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Jupyter
.ipynb_checkpoints/

# Repo-root KG databases only (book-level .dockg/ etc. are tracked as built artifacts)
/.dockg/
/.codekg/
/.ftreekg/

# SQLite transient files (WAL/SHM are runtime-only, can exceed GitHub's 100MB limit)
*.sqlite-wal
*.sqlite-shm

# LanceDB transient files (runtime-only; _transactions/ pruned on compaction, _versions/ rewritten)
**/.dockg/lancedb/**/_transactions/
**/.dockg/lancedb/**/_versions/
**/.dockg/lancedb/**/data/
